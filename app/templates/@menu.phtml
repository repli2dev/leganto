<ul id="links">
	{block #menu}
		{foreach $menu AS $links}
			{foreach $links AS $link}
				<li><a href="{plink $link['url']}" title="{$link['name']}">{$link['name']}</a></li>
			{/foreach}
		{/foreach}
	{/block}
</ul>