@{extends layout.phtml}

@{block #subcontent}
<h1>{_"Opinions on the book"}: {$book->title . (($book->subtitle == NULL) ? "" : ": " . $book->subtitle)}</h1>

<div id="opinions">
	@{widget opinionList}
	{if $opinionCount == 0}
		<p>{_"There are no text opinions, be the one who write the first one!"}</p>
	{/if}
</div>
@{/block}