<style>
    #submenu {
	border-color: #F1C11D;
    }
    #actions {
	border-color: green;
    }
    #submenu ul li, #actions ul li {
	text-align: right;
	padding: 10px 0;
    }
    #submenu li a, #actions li a {
	font-size: 120%;
	color: #865A2C;
	font-size: 15px;
	font-weight: bolder;
    }
    #submenu li a:hover, #actions li a:hover {
	color: #4C3319;
    }
    #submenu .active, #actions .active {
	background-color: #F5D25A;
    }
</style>
<div class="box" id="submenu">
    <ul>
	{foreach $links AS $link}
	{if $link->getArgs()}
	    <li{if $presenter->getAction() == $link->getAction() && $control->equalArgs($link->getArgs(),$presenter)} class="active"{/if}><a href="{$presenter->link($link->getAction(),$link->getArgs())}">{$link->getName()}</a></li>
	{else}
	    <li{if $presenter->getAction() == $link->getAction()} class="active"{/if}><a href="{plink $link->getAction()}">{$link->getName()}</a></li>
	{/if}
	{/foreach}
    </ul>
</div>

{if !empty($events)}
<div class="box" id="actions">
    <ul>
    	{foreach $events AS $link}
			{assign url    => $component->getPresenter()->link($link->getAction(), $link->getArgs())}
			<li class="event {if $presenter->getAction() == $link->getAction()}active{/if}">				
					<a href="{!$url}" {if $link->getConfirm() != NULL}onclick="return (confirm('{!$link->getConfirm()}'))"{/if}>{$link->getName()}</a>
			</li>
	{/foreach}
    </ul>
</div>
{/if}